apiVersion: v1
kind: Service
metadata:
  name: basket-service
  labels:
    app: basket
spec:
  type: ClusterIP
  ports:
    - port: 80
      targetPort: 80
      protocol: TCP
      name: http
  selector:
    app: basket
