apiVersion: v1
kind: Service
metadata:
  name: blazor-webassembly-service
  labels:
    app: blazor-webassembly
spec:
  type: ClusterIP
  ports:
    - port: 80
      targetPort: 80
      protocol: TCP
      name: http
  selector:
    app: blazor-webassembly
