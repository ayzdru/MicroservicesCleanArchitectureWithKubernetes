apiVersion: v1
kind: Service
metadata:
  name: identity-service
  labels:
    app: identity
spec:
  type: ClusterIP
  ports:
    - port: 80
      targetPort: 80
      protocol: TCP
      name: http
  selector:
    app: identity
